---

# Devices we want mapped into the container
ollama_device_mappings:
  amd:
    - "/dev/kfd" # Kernel Fusion Driver
    - "/dev/dri" # Direct Rendering Infrastructure
  nvidia: []
  none: []

# Configure timeout for pulling models
ollama_model_api_timeout: 1800
